<%= form_for(@song_performed_by, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @song_performed_by.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@song_performed_by.errors.count, "error") %> prohibited this song_performed_by from being saved:</h4>

      <ul>
      <% @song_performed_by.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :song_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
			<%= f.collection_select(:song_id, Song.all.sort_by {|obj| obj.title}, :id, :title)  %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :artist_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
			<%= f.collection_select(:artist_id, Artist.all.sort_by {|obj| obj.name}, :id, :name)  %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit "Create association", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
