<%= form_for(@artist_belong_to, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @artist_belong_to.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@artist_belong_to.errors.count, "error") %> prohibited this artist_belong_to from being saved:</h4>

      <ul>
      <% @artist_belong_to.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :artist_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
			<%= f.collection_select(:artist_id, Artist.all.sort_by {|obj| obj.name}, :id, :name)  %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :genre_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
			<%= f.collection_select(:genre_id, Genre.all.sort_by {|obj| obj.name}, :id, :name)  %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit "Create association", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
