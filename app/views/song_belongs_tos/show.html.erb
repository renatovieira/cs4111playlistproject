<div class="page-header">
  <%= link_to song_belongs_tos_path, class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to edit_song_belongs_to_path(@song_belongs_to), class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-pencil"></span>
    Edit
  <% end %>
  <h1>Show song_belongs_to</h1>
</div>

<dl class="dl-horizontal">
  <dt>Song:</dt>
  <dd><%= @song_belongs_to.song_id %></dd>

  <dt>Genre:</dt>
  <dd><%= @song_belongs_to.genre_id %></dd>

</dl>
